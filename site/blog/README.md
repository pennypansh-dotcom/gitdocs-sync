# Blog source files

Add publishable Markdown files to `posts/`. The `Publish website and blog` GitHub workflow generates the public HTML, RSS, and sitemap, then updates the existing `gh-pages` branch.

See `docs/官网博客统一发布流程.md` for the non-code publishing steps and `docs/blog-article-template.md` for the article template.
